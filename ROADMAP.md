# Alfred — ROADMAP v1

Backlog extrait des specs (`spec/`). Suivi simple : cocher au fil de l'eau,
mettre ses initiales dans **Qui**. Les specs restent la source de vérité du *quoi*
et du *comment* ; ce fichier suit le *où on en est*.

Légende : `[ ]` à faire · `[~]` en cours · `[x]` fait · ⚠️ = risque / chemin critique.

---

## 🎯 Risques & chemin critique (à attaquer en premier)

- ⚠️ **Backend AlfredIA** — gate le lancement (proxy + Stripe + metrics). Repo privé `alfred-backend`.
- ⚠️ **Audio système sur Windows** — WASAPI loopback, rien de codé. Spec 03.
- ⚠️ **Whisper par défaut, cross-platform** — build + packaging + modèle embarqué. Spec 04.

---

## Phase A — Backend (repo privé `alfred-backend`) · gate le lancement

| | Tâche | Qui |
|---|---|---|
| [x] | Service Rust/axum + déploiement **Coolify** (Docker depuis Git) + domaine `api.alfred.do-now.io` (Traefik/Let's Encrypt) | UC |
| [x] | **Proxy** `POST /v1/messages` : lookup token, abo actif, allowlist modèles, forward + retry (+ tables `tokens`/`metrics`) | UC |
| [x] | **Stripe** : produit AlfredIA 20 €/mois (+ annuel), sans essai ; Checkout ; webhook (émission/révocation token) | UC |
| [x] | **Souscription loopback** : `/subscribe` + `/subscribe/complete` (nonce/port → token) | UC |
| [x] | **PostgreSQL** (Coolify) : tables `tokens` + `metrics` ; endpoint `POST /metrics` | UC |
| [x] | **`POST /feedback`** → tout en **Postgres** (texte + images BYTEA, consultation SQL ; email/S3 hors v1) | UC |
| [x] | **Secrets** : variables d'env Coolify (clé Anthropic + clés Stripe, chiffrées) | UC |
| [~] | **Essai gratuit 14 jours AlfredIA (backend privé + spec/13, spec/11, feedback tests)** : Checkout Stripe avec `subscription_data.trial_period_days = 14` (✅ fait) ; token émis dès le départ en `trialing` (proxy accepte déjà, ✅) ; wording onboarding + Réglages « Commencer l'essai gratuit » (✅ fait). **Reste** : endpoint de statut détaillé (`trialing` vs `active` + jours restants, desktop ne voit aujourd'hui que « token valide/invalide ») ; tester fin d'essai → `active` et échec paiement → `suspended` (recette sandbox, cf. tâche dédiée) | CF |
| [ ] | **Recette du paiement — 20 €/mois (backend privé `alfred-backend`)** : rejouer `/subscribe` en **sandbox** Stripe (Checkout carte de test → webhook → token → proxy accepte) + simuler `subscription.deleted`/`payment_failed` (`stripe trigger`) → vérifie `suspended`/`revoked` ; puis **un** paiement réel de validation en **prod** une fois la sandbox verte | |
| [x] | **Bouton « Gérer l'abonnement » AlfredIA (backend privé + spec/11)** : backend `POST /subscription/portal` (Bearer token → `customer_id` → session Billing Portal Stripe → `{url}`) + `GET /subscription/portal/done` (page de retour) ; desktop `manage_alfredia_subscription` ouvre l'URL dans le navigateur (`tauri_plugin_opener`) ; bouton dans Réglages → Accès IA, à côté de « ✓ Activé ». **Suppose le Customer Portal activé côté dashboard Stripe** (paramétrage manuel, non testé en sandbox) | UC |

## Phase B — Desktop, moteur

| | Tâche | Qui |
|---|---|---|
| [~] | ⚠️ **Audio système** : Windows ✅ (WASAPI loopback, `system_only` + `mixed`, testé) — reste macOS helper Swift (Tanguy) | UC/T |
| [x] | Durcir la **capture micro** (gérer `i16`/`f32`/`u16` selon le device) | UC |
| [x] | **Feedback live** d'enregistrement : volume (RMS) + timer dans `recording-status-changed` (micro ; system_only/mixed pas encore de volume live) | UC |
| [x] | ⚠️ **Whisper** : activer la feature par défaut + **embarquer le modèle `small`** + packaging Windows | UC |
| [x] | Transcription : **stocker la langue** détectée (bug) ; écrire dans `alfred-raw/` avec frontmatter (`for_recording`) | UC |
| [x] | **Progression de transcription (spec/04, feedback tests)** : `transcription-progress { recording_id, percent }` branché sur `whisper-rs::set_progress_callback_safe` (passe unique) + agrégat pondéré par tranche (parallèle, spec/17 §5) ; affiché sur l'indicateur d'état (« Je prends note… {n} % ») + `/recording` (barre) + toast de la visite guidée — pas de doublon dans le bandeau topbar (spec/10) | CF |
| [x] | IA : passer aux modèles **Sonnet 5 / Haiku 4.5** ; **sorties structurées** ; **routage 2 modes** ; thinking off (fait pour l'ingestion ; chat.rs pas encore aligné sur `thinking: disabled`) | UC |
| [x] | **Ingestion** (fusionnée) : 1 appel → compte-rendu (`alfred-intelligence/`) + tâches (`Todo.md` + SQLite en double écriture, cf tâche suivante) | UC |
| [x] | **Todos → vault** : `Todo.md` seule source de vérité — table SQLite supprimée (migration 007), double écriture de l'ingestion retirée, commandes refondues sur le fichier (id = titre normalisé), code mort frontend (todoStore/TodoItem) supprimé | UC |
| [x] | Notes : frontmatter **`project` + `participants`** (peuplés par l'ingestion) ; structure `alfred-raw`/`alfred-intelligence` ✅ ; **regroupement par projet** (vue « Projets » dans l'arbre, dossiers virtuels — `get_notes_by_project`) ✅ ; **plus de `.claude`/skills** ✅. *Rangement physique par projet = hors v1* | UC |
| [x] | **Brief quotidien** (`generate/get_daily_brief`) | UC |
| [x] | **Chat** : historique multi-conversations + liste (persistance SQLite, migration 006 ; liste/reprise/suppression dans le panneau chat) | UC |
| [x] | **Metrics** : `install_id` anonyme + envoi des événements (`install_created`/`app_launched`/`recording_completed`/`ingestion_completed`/`ai_request`) — tous câblés. Manquait la clé anti-spam `x-metrics-key` côté app : embarquée à la compilation (`option_env!`) via le secret CI `ALFRED_METRICS_APP_KEY`, doit correspondre au `METRICS_APP_KEY` Coolify | CF |
| [x] | **Contexte interne** : note vault `Contexte Alfred.md` + template + injection ingestion + Settings (spec 16) | T |
| [x] | **Transcription live** (spec 16) : **abandonnée** — code retiré (revert `transcription/live.rs`, session acteur, événements `live-*`, `save_live_note`/`get_live_session`, miroir CodeMirror) ; le contexte interne subsiste | T/UC |

## Phase B3 — Qualité de transcription (spec 17)

| | Tâche | Qui |
|---|---|---|
| [x] | **Qualité de décodage** (spec 17) : beam search + seuils anti-hallucination + threads relevables dans `run_whisper` ; langue forçable par enregistrement | UC |
| [x] | **Glossaire dérivé** (spec 17) : `generate_glossary_from_context` (Claude) depuis `Contexte Alfred.md` → `config.transcription_glossary` → `initial_prompt` de `run_whisper`. **Régénéré automatiquement** (débouncé 4 s) quand la note de contexte est éditée (`update_note_file` → `schedule_glossary_regen`) + à l'onboarding + bouton manuel | UC |
| [x] | **Ingestion augmentée** (spec 17) : ingestion 2 temps (analyse → résolution groupée + réécoute segment → finalisation) + enrichissement auto « Appris automatiquement » dans `Contexte Alfred.md`. Backend (`analyze_transcription`/`finalize_ingestion`, event `clarifications-ready`) + **écran `/resolve`** (texte éditable CodeMirror, cartes appliquer/ignorer, réécoute WAV via `read_recording_wav`) + bandeau d'invite. **Flag `augmented_ingestion` — ON par défaut** (désactivable via `set_config('augmented_ingestion','false')`). Bouton **« Vérifier / corriger »** sur les notes d'enregistrement (Notes) → relance `analyze_transcription` et rouvre `/resolve` (reprise après quitter, la session live n'étant pas persistée) | UC |
| [x] | **Onboarding — contexte à la voix** : visite guidée où le 1er enregistrement (téléprompteur) crée `Contexte Alfred.md` → `build_context_from_transcription` (structure la note) + 1er glossaire ; event `context-status-changed`. `start_recording(purpose)` + migration 010 `recordings.purpose` + routage `process_job`. Front : `Teleprompter`, étapes tour reshapées, suggestion chat contexte (spec 17/13) | UC |
| [x] | **Transcription parallèle par tranches** (spec 17 §5) : CPU pur trop lent sur les longs fichiers (1H ≈ 30 min en passe unique). Découpe aux silences (>15 min) + workers parallèles (modèle partagé, `state`/worker) + ré-injection du glossaire par tranche + recollage des timestamps. ~1,5–2,5×, cumulable avec modèle/beam. GPU = hors v1 | UC |
| [x] | **`context_addition` — resserrer durable vs ponctuel (spec 17 §3, feedback tests)** : Claude proposait aussi des faits ponctuels/propres à la réunion dans « Appris automatiquement », qui polluent le contexte général (et donc le glossaire). Correctif purement prompt (`ANALYZE_SYSTEM` + description du champ `context_additions` dans `analyze_tool`, FR/EN, `src-tauri/src/ai/mod.rs`) : distinction durable/ponctuel + exemples (✅ rôle/relation, activité de l'entreprise, vocabulaire, projets en cours · ❌ planning/rdv, décisions de la réunion, chiffres du jour) + test mental « utile dans 3 mois pour un futur enregistrement ? » | UC |
| [x] | **Contexte à la voix — 2 correctifs qualité (spec 16, feedback tests)** : (a) **empilement en double** — un re-build vocal (Recommencer/2ᵉ prise) empilait une structure entière sous `## Appris à l'oral (date)` au lieu de mettre à jour ; `write_spoken_context` (`notes/context.rs`) **remplace désormais en place** les 4 sections canoniques (reconnues FR/EN via `canonical_slot`), idempotent, en préservant titre/intro/`## Appris automatiquement`/sections perso à leur position d'origine (+ auto-cicatrise les doublons déjà présents) ; (b) **contenu = faits, pas l'avis de Claude** — sur une transcription pauvre, `build_context_from_transcription` écrivait son analyse (« la transcription est trop courte… ») au lieu de laisser vide, et dupliquait des paraphrases sur une bonne prise. Correctif purement prompt (`CONTEXT_BUILD_SYSTEM` + `submit_context`, FR/EN) : jamais de méta-commentaire, section vide si rien de fiable (y compris `entreprise`), une formulation consolidée par champ | UC |

## Phase B4 — Partage de notes (spec 18)

| | Tâche | Qui |
|---|---|---|
| [x] | **Backend partage** (spec 18) : `POST /share` + `GET /s/{slug}` (rendu comrak **mode sûr**, `noindex`, CSP stricte, CSS inline clair/sombre) + `PUT`/`DELETE /share/{slug}` (`manage_token` hashé) + migration `0006_shares` (Postgres) ; auth clé app optionnelle (byo + AlfredIA) ; tests XSS/GFM | UC |
| [x] | **Desktop partage** (spec 18) : composant `ShareButton` (Notes + Tâches) + commandes `share_note`/`unshare_note`/`get_share_link` (+ `share_todos`/`get_todos_share_link`/`unshare_todos`) + migration locale `011_note_shares` + confirmation 1re fois (le contenu quitte le vault) + copie presse-papier ; re-partage = **même URL** (PUT) | UC |

## Phase C — Desktop, UX & écrans

| | Tâche | Qui |
|---|---|---|
| [x] | **Enregistrer pendant transcription/analyse (spec/03, feedback tests)** : le backend le permet déjà (`start_recording` ne bloque que la dictée ; worker de transcription en file ; micro libre au stop, `recording_id` enfilé avant retour). Blocage = **front** qui confond capture ↔ traitement (`recStatus` reste non-`idle` pendant la transco ; `AlfredLogo.handleClick` **et** `Dashboard.tsx::HeroCard` ne démarraient que si `idle`). Fix (front only) : nouveau dérivé `canStartNewTake` (`idle`/`processing`/`error`) remplace le test `=== "idle"` aux deux endroits — seule une capture réelle (`recording`/`paused`) bloque désormais ; les 2 indicateurs coexistent déjà (`alfredStatusStore`, spec/10). Transcriptions **séquentielles** (file) assumées. **Complexité : faible, surtout front** | UC |
| [x] | **Bug — overscroll global du shell (spec/10, feedback tests)** : partout où la vue n'a pas son propre scroll, la **page entière se tirait** (rubber-band, marges vides haut/gauche, revient au relâchement). `html, body, #root` (`styles.css`) avaient `height:100%` **sans** `overflow:hidden`/`overscroll-behavior` — corrigé (`overflow:hidden; overscroll-behavior:none` sur les 3, + `#root { position:fixed; inset:0 }`) ; `AppInner` (`App.tsx`) avait déjà `height:100%; overflow:hidden` sur sa racine, donc seuls les panneaux internes (arbre, Kanban, chat…) scrollent, chacun dans son propre conteneur borné | UC |
| [x] | **Accueil « Alfred »** (spec/10) : brief ✅ + bloc tâches dépliable ✅ + input chat + exemples ✅. **Fusion cible faite** — « Alfred » prend la place d'« Aujourd'hui » (`/`), layout **2 colonnes** (gauche : conversation Alfred, `ChatPanel` complet avec son historique ; droite : carte d'enregistrement + brief + tâches). `/ai-actions` retirée (nav + route + écran `AIActions.tsx`) | CF |
| [x] | **Indicateur d'état = où Alfred travaille (spec/10, feedback tests)** : (a) le **point ambre** d'une note = la note qu'Alfred **traite** (transcription/analyse/contexte), plus « note sélectionnée » (highlight suffit) ; (b) **indicateur majordome cliquable** → navigue vers la cible en cours. Cible active dans `alfredStatusStore` (`targetPath`/`targetRoute`/`recordingId`), alimentée par `transcription-complete` (qui porte désormais `note_path`) | CF |
| [x] | **Indicateur d'état** (topbar, labels majordome) + **bandeau d'enregistrement** (timer + volume live + stop) | UC |
| [x] | Déclenchement via **logo** (hover micro) + **page de guidage** d'enregistrement (`/recording`, conseils de captation éditables) | UC |
| [x] | **Logo bouton d'enregistrement décalé (spec/03, feedback tests)** : l'ancien `alfred-logo.png` avait un fond blanc visible dans les coins arrondis + un contenu non centré (portrait en haut, mot « ALFRED » en bas). Remplacé par 2 nouveaux assets fournis, fond transparent, utilisés **tels quels** (aucun recadrage/traitement d'image de notre côté) : `alfred-logo-minimal.png` (portrait seul, sans mot-symbole) pour les petites icônes (visite guidée, page de guidage) et `alfred-logo.png` (avec « ALFRED ») pour les grands formats (onboarding) **et le bouton d'enregistrement de la sidebar** — composant partagé `AlfredAvatar` (prop `variant`) | CF |
| [x] | **Encadré rouge d'enregistrement mal ajusté au logo (spec/03, feedback tests)** : le cadre rouge (`border` + `border-radius` CSS fixe) ne suivait pas le contour réel de l'asset transparent (arrondi type « squircle »), laissant un décalage visible aux coins. Remplacé par un `filter: drop-shadow(...)` (4 directions) sur l'image elle-même, qui épouse le contour alpha réel quel que soit son tracé, au lieu d'un cadre géométrique séparé | CF |
| [x] | **Import de fichier audio** (spec/03) : commande `import_audio_file` (picker WAV → copie `recordings/<uuid>.wav`, `source='import'`, réutilise la file de transcription via `transcription::enqueue_job` partagé avec `stop_recording`) + migration 009 (CHECK `source` élargi) + bouton « Importer un audio » sur `/recording` **et sur l'accueil** (sous la carte d'enregistrement, état repos — `/recording` n'étant atteignable qu'en enregistrant) | UC |
| [x] | **Import audio — mieux intégré à l'interface (spec/03)** : garder la fonction (picker `.wav` → même file de transcription) mais retravailler le placement — aujourd'hui second bouton à côté de « Démarrer l'enregistrement », à incorporer dans les points d'entrée existants plutôt qu'un bouton séparé. **Retour (feedback tests)** : gardé uniquement sur la **carte d'enregistrement** de l'accueil, retiré du **logo** sidebar (qui ne doit porter que le déclencheur d'enregistrement) | CF |
| [x] | **Nav** : retirer routes mortes Réunions / Calendrier + barre de recherche ; ajouter **Feedback**. `/ai-actions` gardée à part (écart documenté spec/10 — historique chat pas encore fait) | UC |
| [x] | **Onboarding** refonte (2 slides, détection vault + scaffolding dossiers, choix accès IA, test micro) + **tournée guidée** post-onboarding (vrai enregistrement → transcription → ingestion → tâches/notes → question à Alfred) — *tracker générique, détails éclatés dans les lignes spec/13 dédiées ci-dessus/dessous (61, 93, 94, 99…), toutes `[x]`* | UC |
| [x] | **Bouton one-shot « Supprimer les données de démo » (spec/13/10, feedback tests)** : notes de démo marquées `alfred_seed: true` (frontmatter brut) ; tâches ciblées par titre exact (FR+EN) plutôt qu'un marqueur de ligne (aurait fui dans l'affichage) ; conversation de démo ciblée par id retenu en config ; cmd `has_starter_content`/`delete_starter_content` — présence vérifiée **en direct** (pas un drapeau figé) ; bandeau en haut de la page Alfred (`Dashboard.tsx`), réévalué sur `notes-updated`/`todos-updated`, disparaît après clic ou suppression manuelle | UC |
| [x] | **Internationalisation FR/EN (spec/21, feedback tests)** — gros chantier transverse : `app_language` (`fr`\|`en`) + **choix à l'installation** (étape 0 onboarding) + réglage Langue ; catalogues de traduction (aucune chaîne UI en dur, hook `t()`) ; **langue des sorties IA** paramétrable (≠ langue UI, ingestion/glossaire/contexte/chat/brief) ; défauts localisés (contenu de démarrage) ; **découpler les sections `Todo.md`** de leur libellé (clés stables) pour ne pas casser le parsing. **Dette restante disclosed** (spec/21) : messages d'erreur backend via codes (catalogue `errors.ts` de départ non branché), titres `Contexte Alfred.md` restés FR-only, nouvelles sections `Todo.md` écrites en FR-only | UC |
| [x] | **i18n — titres contexte & compte-rendu localisés (spec/16/05, feedback tests EN)** : titres du template + de `build_context_from_transcription` (`submit_context`) localisés selon `app_language` (`notes/context.rs`, `ContextTitles`) ; titre généré du compte-rendu (`## Points clés` → `## Key points`) localisé (`run_ingestion_core`) ; sections reconnues par **clé interne** (FR **ou** EN, `context_has_content`/`has_learned_heading`) ; nom de fichier `Contexte Alfred.md` reste stable | UC |
| [x] | **i18n — langue des sorties IA (spec/05, feedback tests EN)** : déjà en place via `language_instruction(db)` (helper partagé, appendu à `INGESTION_SYSTEM`/chat/brief/glossaire/contexte) — vérifié à cette occasion, rien à ajouter côté prompt IA. Le vrai residu était le titre `## Points clés` codé en dur (traité ci-dessus) | UC |
| [x] | **Bug — transcription EN sortie en FR (spec/17/04, feedback tests)** : la phrase d'enrobage du glossaire n'était pas figée côté Rust mais son EXEMPLE dans `GLOSSARY_SYSTEM` l'était (« Transcription en français… ») → biaisait Claude à toujours l'imiter. Corrigé : deux exemples FR+EN + consigne explicite « n'écris pas systématiquement en français ». Ajouté aussi : `resolve_whisper_language` force `language` quand `language_hint` est explicite (au lieu de `auto`) et défaut EN si `app_language=en` sans hint | UC |
| [x] | **RÉOUVERT — sorties Claude en FR malgré transcription EN (spec/05/13/17, 2e test — TRANCHÉ)** : preuve `/resolve` réunion EN → citation d'origine **en anglais** (Whisper OK) mais **propositions/faits en français**. Cause = **Claude**, pas Whisper — inférence + schémas tout-français. **Corrigé** : `recording_language(db, recording_id)` lit la langue **réellement détectée** (`transcriptions.language`, posée par Whisper) plutôt que de la deviner ; `language_directive(lang)` — consigne impérative « Write ALL fields / your entire answer in {lang} » — appliquée à `call_ingestion`, `call_analyze` (n'avait aucune consigne avant) et `build_context_inner`. **Descriptions de champs des tools** (`submit_ingestion`, `submit_clarifications`, `submit_context`) **alignées sur la langue cible** (EN quand `lang="en"`) — noms de champs inchangés (clés serde internes). Titres localisés du contexte (spec/16) restent sur `app_language`, volontairement distinct de la langue du contenu | UC |
| [x] | **Pastille de travail pendant analyse/ingestion (spec/10/17, feedback tests)** : `alfredStatusStore` gagne `verifying` (« Je vérifie… », pastille maintenue sur la note brute pendant `call_analyze`) et `reviewing` (« Un point à vérifier », `targetRoute: "/resolve"` sur `clarifications-ready` au lieu d'un retour silencieux à `idle`) ; `report_path` ajouté à `ingestion-status-changed{phase:"summary"}` pour déplacer la cible vers le compte-rendu dès qu'il est écrit | UC |
| [x] | **Bug — brief quotidien en FR sur app EN (spec/05, feedback tests)** : sans tâche/note notable, le CONTENU envoyé à Claude (en-têtes + libellés de repli « Aucune tâche en attente. »…) était codé en dur en français — `language_instruction` lui fait suivre la langue **du contenu fourni**, qui n'était donc que ce texte français d'échafaudage. Libellés localisés selon `app_language` (`generate_daily_brief`) | UC |
| [x] | **Écran de fin d'onboarding — habillage « pré-réunion » (spec/13)** : première passe = aperçu statique de la carte « Démarrer l'enregistrement ». **Revu (feedback tests)** : cet aperçu ne correspondait pas au vrai bouton et n'apportait rien — retiré du wizard (`Onboarding.tsx` « ready » redevient un panneau simple). À la place, **nouvelle étape `record-cta`** dans la visite guidée (`GuidedTour.tsx`, juste avant la clôture) : vrai spotlight simultané sur le **logo Alfred** (nouvelle cible `alfred-logo-button`) et sur la **carte d'enregistrement de l'accueil** (`hero-card`, déjà enregistrée) — `Spotlight` gagne un `children` optionnel (pas de halo « aveugle » cliquable quand il n'y a pas de bulle de texte) | UC |
| [x] | **Téléchargement du modèle Whisper pendant l'onboarding (spec/13, Tanguy)** : étape « Modèle de transcription » dans le wizard (passable, Suivant gelé pendant le téléchargement, poursuite en arrière-plan) + **gestionnaire de modèles** dans Réglages (composant partagé `WhisperModelPicker` : catalogue tiny→large-v3-turbo avec Small recommandé, pré-téléchargement, annulation, suppression, « Utiliser » restreint aux modèles téléchargés) ; cmds `list_whisper_models`/`cancel_model_download`/`delete_whisper_model`, events `download-progress {model,…}`/`download-complete`/`download-error` ; bonus : `transcription-failed` réellement émis + bannière d'échec avec lien Réglages (le trou silencieux de spec/04) | T |
| [x] | **Visite guidée — retours tests (spec/13)** : (a) étape « Contexte prêt » = **un seul bouton « Revoir/corriger »** (retirer « Continuer ») ; (b) **meubler la transcription** par la visite de l'app (Notes → Tâches → Graphe → Questions à Alfred & enregistrer), pop-up « Alfred vous connaît, vérifiez » **dès `context-status-changed: done`** (interrompt la visite) ; (c) après correction → **clôture** « Vous êtes équipé » | CF |
| [x] | **Visite guidée — écran de correction contexte (spec/13/17)** : « Revoir/corriger » ouvre l'**écran `/resolve` en mode contexte** (4 sections éditables + réécoute WAV via `note_title` porté par `context-status-changed` + Valider — la sauvegarde passe par `update_note_file`, qui régénère aussi le glossaire) | CF |
| [x] | **Visite guidée — ne pas interrompre (spec/13, feedback tests)** : la pop-up « Contexte prêt » ne doit **plus s'afficher dès `context-status-changed: done`** — on **mémorise** l'événement (drapeau + récap) et on laisse l'utilisateur **finir toute la visite** ; la pop-up n'apparaît **qu'à la fin** (immédiatement si le contexte est déjà prêt, sinon on attend l'event sur l'indicateur d'état) | CF |
| [x] | **Correction contexte = MÊME écran que réunion (spec/13/17, feedback tests)** : supprimer la page/variante `/resolve` spécifique à l'onboarding ; le « Revoir/corriger » du contexte passe par **exactement le même écran et le même parcours** de vérification qu'un vrai enregistrement (seul le contenu injecté diffère) | CF |
| [x] | **Bug — écran blanc en fin de replay onboarding (spec/13, feedback tests)** : « Revoir l'introduction » → clic « Terminer » → **fond uni sans sidebar** (= garde `onboarded===null` d'`App.tsx`). Cause confirmée : `forceOnboarding` lisait `sessionStorage` à chaque rendu au lieu d'être un état React — sur une install déjà onboardée, `setOnboarded(true)` est un no-op (même valeur) donc rien ne force le nouveau rendu qui sortirait de la branche `forceOnboarding`. Fix : `forceOnboarding` en `useState`, remis à `false` explicitement dans `finishOnboarding`/`finishOnboardingReplay` | CF |
| [x] | **Finalisation gated par la vérification (spec/17/05, feedback tests)** : le **compte-rendu + tâches ne sont générés qu'APRÈS validation** dans `/resolve` — retirer l'auto-enchaînement « rien à corriger → finalise tout seul » ; l'étape Résolution est **toujours** présentée (plus courte si rien à corriger), et c'est le **Valider** qui déclenche `finalize_ingestion` (idem contexte via `build_context_from_transcription`) | CF |
| [x] | **RÉVISION — `/resolve` seulement si nécessaire (spec/17/05, feedback tests)** : **revient sur** la tâche ci-dessus. `run_ingestion_for_recording` finalise désormais **directement** (`finalize_ingestion`, sur le texte brut) dès que l'analyse ne renvoie **aucun** `transcription_fix`/`unclear_sentence`/`unassigned_task` — `/resolve` n'est présenté **que** s'il y a réellement des clarifications. Les `context_additions` (auto « Appris ») restent toujours écrits, avec ou sans clarifications — ils ne comptent jamais comme « à vérifier » | UC |
| [ ] | **Contexte vocal — remplacer (pas empiler) + pas d'avis de Claude (spec/16/13, feedback tests)** : après onboarding, `Contexte Alfred.md` cumule **2 structures complètes** (bloc + `## Appris à l'oral` re-dupliqué) car re-lancer le build passe par le chemin *append*. → `build_context_from_transcription` **REMPLACE** les 4 sections canoniques (idempotent), plus jamais d'empilement de structure complète ; seul `## Appris automatiquement` (post-ingestion, dédup) s'ajoute. **Et** `CONTEXT_BUILD_SYSTEM`/`submit_context` : **aucun méta-commentaire** (« la transcription est trop courte… » écrit dans la note), **section vide si rien** (dont `entreprise`), **concision + dédup** (plus de 3 paraphrases / 3 listes de vocab) | |
| [ ] | **Contexte — n'ajouter que du DURABLE (spec/17 §3/§4, feedback tests)** : resserrer le critère de `context_addition` dans l'analyse (`ANALYZE_SYSTEM` + description du champ) — ne proposer au contexte que les faits **durables/réutilisables** (qui sont les gens/entreprises + rôle/relation, ce que fait l'entreprise, vocabulaire, projets en cours). **Exclure le ponctuel** (planning « prochaine réunion avec Hugo », décisions/actions du jour) → reste dans le compte-rendu / tâches, jamais dans « Appris automatiquement » (sinon pollue contexte + glossaire). Test : « encore utile dans 3 mois ? » | |
| [x] | **Vérifications persistantes + accès par la note (spec/17/07/10, feedback tests)** : nouvelle table **`pending_clarifications`** (migration 013, clé `recording_id`) — l'analyse déjà payée survit à la navigation, à un **nouvel enregistrement** et à un redémarrage ; commandes `list_pending_clarifications`/`get_pending_clarification` + event `pending-clarifications-changed`. **Icône « à vérifier »** sur la note (arbre Notes **et** Récents, `MdFactCheck`) → clic **charge l'analyse persistée** (`resolveStore.loadPersisted`) et ouvre `/resolve` **sans ré-appeler `analyze_transcription`** (le bouton « Vérifier/corriger » reste le chemin volontaire de ré-analyse). Disparaît à la finalisation (`finalize_ingestion` supprime la ligne + réémet l'event). `VaultNode` gagne `recording_id` (parsé comme `status`) pour le croisement côté arbre — **vue Projects non couverte** (hors scope, seuls « l'arbre » et « Récents » étaient visés par le texte de la spec) | UC |
| [x] | **Visite guidée — contrôles téléprompteur (spec/13, dépend spec/03)** : **Pause/Reprendre** pendant la prise ; « J'ai terminé » → état « prise terminée » avec **Recommencer** (jette la prise) et **Continuer** (lance seulement là la transcription) — cmds `pause_recording`/`resume_recording`/`discard_recording` | CF |
| [x] | **Visite guidée — annoncer le traitement + expliquer le point d'état (spec/13, demande utilisateur)** : entre la présentation et la visite de l'app, deux nouvelles étapes plutôt qu'un enchaînement silencieux — carte **« Je m'occupe de tout »** (j'écoute/je reviendrai vers vous) puis **spotlight sur le point d'état de la sidebar** (`AlfredLogo`, toujours monté) expliquant ce qu'il signifie, **avant** de naviguer vers `/notes` et d'entamer Notes → Tâches → Graphe → Chat | UC |
| [x] | **Enregistrement — arrêt interruptible + traitements aval optionnels (spec/03)** : bouton **Annuler** pendant la prise (`cancel_recording`, jette le WAV) ; « Terminer » → panneau de revue **Supprimer** / **Continuer** (modal global, quel que soit l'écran) ; `stop_recording` ne lance **plus** l'aval → `process_recording(id, {transcribe, summary, tasks})` ; cases **Transcription / Compte-rendu / Tâches** cochées par défaut (transcription requise pour les 2 autres). Panneau `RecordingReview` partagé avec le téléprompteur (spec/13). Pause = frames jetées (mic + loopback), chrono figé | CF |
| [x] | **Ingestion — sortie découplée (spec/05, dépend spec/03)** : `run_ingestion_for_recording`/`_for_note` prennent `{summary, tasks}` et n'écrivent que les sections demandées (1 appel à sortie conditionnelle) ; `summary=false && tasks=false` → pas d'appel IA | CF |
| [x] | **Source audio mixte par défaut (spec/03/11, feedback tests)** : `recording_source` défaut `mixed` (micro + système) ; les points de départ lisent la config au lieu de `"mic_only"` codé en dur ; **repli auto sur `mic_only`** si système indispo (macOS sans helper Swift) ; contexte à la voix reste `mic_only`. **Bug corrigé (2e passe, feedback tests)** : le défaut n'avait en réalité JAMAIS pris effet — migration 001 insère littéralement `('recording_source','mic_only')` au 1er lancement, donc `get_config` ne renvoie jamais `NULL` et le repli `?? "mixed"` côté front n'était jamais atteint, sur aucune install. Migration 014 corrige la valeur (ne touche que les lignes encore à `mic_only`) | CF/UC |
| [x] | **Enregistrement — retirer le panneau de sélection, tout auto (spec/03, feedback tests)** : supprimer le `RecordingReview` post-arrêt (cases Transcription/Compte-rendu/Tâches) côté **réunion** → après « Terminer », lancer **auto** transcription + compte-rendu + tâches (`{summary:true, tasks:true}`). **Garder** la vérif `/resolve` (point 23), le bouton **Annuler** pendant la prise, et la suppression après coup. **Découpler** du téléprompteur de contexte, qui garde sa revue **Recommencer/Continuer** (spec/13). Le sélecteur `{summary,tasks}` reste côté backend (non exposé UI) | CF |
| [x] | **Note de contexte — bug blocs vides (spec/16)** : `write_spoken_context` doit **remplacer** le template (intro + titres vides ne comptent pas comme du contenu) au lieu d'empiler sous « Appris à l'oral » — fix `context_has_content` (l'intro sur 2 lignes passait le filtre) + tests | CF |
| [x] | **Contenu de démarrage / seed (spec/13)** : semé à la fin de l'onboarding (`seed_starter_content`, idempotent, gardé, flag `starter_content_seeded`) — tâches checklist (sections/assignation/une cochée), 2 notes de démo (frontmatter project+participants → graphe/Projets), fausse conversation chat ; question suggérée déjà en place (spotlight chat) | CF |
| [x] | **Dictée vocale du chat (spec/07b, feedback tests)** : bouton micro dans la barre de saisie (`ChatPanel` + `ChatTeaser`) → capture micro éphémère → Whisper (glossaire) → texte inséré dans le champ (éditable, pas d'envoi auto). Cmds `start_dictation`/`stop_dictation()->String` (WAV temp supprimé, pas de note/recordings/ingestion), event `dictation-status-changed`. Désactivée pendant un enregistrement de réunion (capture = singleton) | CF |
| [x] | **Audit des liens internes & navigation (spec/23, feedback tests)** : (a) `renderInlineMd` accepte un `onNavigate` — cartes Kanban / fiche tâche / brief **cliquables** (fini les spans morts « no target ») ; (b) **gestionnaire unique** `useInternalLink` (`src/utils/useInternalLink.ts`) : `wikilink:`→`openNoteByRef`→Notes, **`task:<titre normalisé>`**→`/tasks?focus=`→**carte surlignée** (scroll + halo 2,5 s, filtres réinitialisés si besoin), `http(s)`→externe ; (c) **compte-rendu → section « Tâches » cliquable** (`task:` généré côté Rust via `normalize_title`, complète la provenance tâche→CR) ; (d) résolution robuste (nom fichier insensible casse/accents ; tâche par identité exacte, générée Rust des deux côtés) et **échec = toast visible** (`toastStore`/`Toast.tsx`) ; couvre preview note / brief / Kanban / fiche tâche / sources chat (déjà bons, vérifiés) / Récents (déjà bon, vérifié). **Non fait** : repli titre frontmatter pour `wikilink:` (gap connu, documenté spec/23), alignement du graphe (déjà fonctionnel autrement, pas nécessaire). **2 correctifs (feedback tests, 2e passe)** : (e) fiche tâche — le bouton « Ouvrir la note » (provenance) réimplémentait sa propre résolution sans le toast de secours (clic mort silencieux) → délègue maintenant à `useInternalLink` comme le reste ; `findNodeByRef` gagne un repli miroir de `sanitize_filename` (Rust) pour les titres contenant `:`/`/` (très courant en français, ex. « Point projet : Atlas ») ; (f) la **preview de note** (`NoteEditor`/CodeMirror, un renderer entièrement séparé de `BriefingContent` que la 1ʳᵉ passe n'avait pas touché) traitait tout lien non-`http(s)` comme un wikilink → un `task:` cliqué depuis une note ne faisait rien. `markdownLivePreview` gagne un `onNavigate` dédié aux schémas non-note | UC |
| [x] | **Alfred agentique — outils d'action (spec/22, feedback tests)** : boucle de chat (`chat.rs`) étendue avec 15 **outils de mutation** (`ai/agent_actions.rs`) — Notes : create/append/update_metadata/archive/unarchive/rename ; Tâches : create/complete/move/update/dismiss — réutilisant les commandes existantes. **Règle d'or : « supprimer » = ARCHIVER** (note→`status:archived`, tâche→`Archivé`) — **aucune suppression dure** exposée (`delete_note_file`/`delete_starter_content` pas dans la whitelist). **Lot/écrasement** (`archive_notes_bulk`/`unarchive_notes_bulk`/`dismiss_tasks_bulk`/`rewrite_note_body`) : jamais exécutés par la boucle — `ProposedAction` renvoyée au front, carte Appliquer/Annuler dans le chat (esprit `/resolve`), résolue **localement** (`confirm_agent_action`, sans repasser par Claude — décision explicite : pas de round-trip conversationnel). Unitaire = exécution directe + compte-rendu dans la réponse. Garde-fous : `resolve_note_path`/`ensure_inside_vault` confinent toute résolution de chemin au coffre (corrige aussi une fuite existante dans `read_note`, qui acceptait un chemin absolu littéral sans vérification) ; whitelist stricte (pas de nouvel outil hors périmètre). **Bonus** : system prompt + tous les schémas d'outils du chat (existants + nouveaux) alignés sur `app_language` (même correctif que spec/05/17, décision explicite du lot). **Hors v1** : Réglages/app. **Bug corrigé (2e passe, feedback tests)** : les 4 sections `Todo.md` restent stockées en français quelle que soit `app_language` (limitation connue, spec/21, hors périmètre à réécrire) — Alfred recopiait ce libellé brut dans une réponse anglaise (« moved to the "Fait" column »). `chat_system(lang)` porte désormais une consigne explicite de traduction. Résidu non corrigé : les requêtes `search_notes` que Claude choisit restent parfois en français (même cause racine, visible dans `chat-progress`) | UC |
| [x] | **Récents — `Contexte Alfred.md` en tête après (presque) chaque ingestion (spec/07/16, feedback tests)** : `finalize_ingestion` auto-écrit les faits appris dans cette note quasi systématiquement, avançant sa mtime devant le compte-rendu qu'on vient de produire. `list_recent_notes` gagne un `exclude_paths` (résolu via `context_note_path`) appliqué à Récents **et** au brief quotidien. **Étendu (2e passe, feedback tests)** : `Todo.md` (`todo_file_path`) exclue de la même façon — déjà accessible via la page Tâches | UC |
| [x] | **`/resolve` — 5 correctifs de lecture/persistance/duplication (spec/17, feedback tests)** : (a) **lecture/pause/reprise du WAV** — le clic lançait l'audio en entier sans moyen de l'arrêter ; `useSegmentPlayer` suit l'état de lecture réel, le bouton actif devient Pause + un 2e bouton « reprendre depuis le début » (flèche circulaire) ; (b) **réécoute cassée une fois la note archivée** — `read_recording_wav` résolvait par le TITRE de la note ouverte (juste tant que c'est la brute) ; résout désormais par `recording_id` (`find_note_by_recording_id`, déjà utilisé pour l'archivage) — fonctionne quelle que soit la note ouverte ; (c) **doublon de compte-rendu à la ré-vérification** — `run_ingestion_core` créait toujours une note (suffixée sur collision) ; cherche désormais l'existante par `recording_id` (`find_intelligence_note_by_recording_id`) et **met à jour en place** si trouvée ; (d) **croix de la bannière « N points à vérifier »** — appelait `finalize_ingestion` (lent, écrit sans revue) puis perdait l'accès à `/resolve` (la ligne persistée était supprimée par la finalisation) ; masque désormais **seulement** la bannière, la ligne `pending_clarifications` et l'icône « à vérifier » restent ; (e) **suppression des données de démo trop large** — le retrait des tâches semées comparait par sous-chaîne (`ligne.contains(titre)`), une tâche réelle contenant par coïncidence un titre semé partait avec ; `delete_starter_content` retire désormais par identité exacte (nouveau `todo_md::remove_task`) | UC |
| [x] | **Capsule « Je note les tâches » (spec/05+10, feedback tests)** : `ingestion-status-changed` porte une **phase** (`analyzing`→`summary`→`tasks`→`done`) ; 5ᵉ label majordome « Je note les tâches… » (1 appel IA → phase tâches brève mais honnête) ; vaut aussi en ré-ingestion (single + lot) | CF |
| [x] | **Graphe — lien transcription ↔ compte-rendu (spec/07c, feedback tests)** : `build_graph` relie **nativement** les notes partageant le même `recording_id` (le graphe n'utilisait que wikilinks/tags ; nom daté commun → wikilink ambigu) | CF |
| [x] | **Notes — nommage par sujet + types à l'œil (spec/05+07, feedback tests)** : champ `titre` (sujet) dans `submit_ingestion` → compte-rendu nommé par sujet (plus la date) ; **icône de type** (transcription/compte-rendu/tâche/contexte/note) dans l'arbre **et** les Récents (dérivée de `type`+dossier) ; Récents = icône + nom + date secondaire | CF |
| [x] | **Notes — mode Projets éditable + paire (spec/07, feedback tests)** : champ **Projet (multi-sélection)** + **Participants** dans Properties (combobox `list_projects` + autocomplétion + créer) ; **glisser-déposer** sur un groupe de projet ; `project` = **LISTE** (multi-projet, note sous chaque projet) ; **paire transcription+compte-rendu** regroupée via `recording_id` | CF |
| [x] | **Notes — tags : liste + autocomplétion (spec/07, feedback tests)** : Properties affiche les tags existants (`list_tags`) + autocomplétion (taper `te` → `test`), clic pour ajouter | CF |
| [x] | **Conseils de captation par type (spec/03, feedback tests)** : plusieurs modèles selon le type (note perso / réunion client / one-to-one / équipe / libre), chacun avec phrase d'ouverture + conseils ciblés ; sélecteur sur `/recording` ; `capture_tips` → dict par type `{opener, tips[]}`, éditable | CF |
| [x] | **Refonte Kanban de la page Tâches (spec/06, feedback tests + demande users)** : vue Kanban sur `Todo.md` (colonnes = sections Prioritaire/En cours/À faire/Archivé) ; glisser-déposer entre colonnes → `move_todo(id, section, position?)` ; cartes lisibles (responsable, badge échéance coloré, coché) ; ajout rapide + compteur par colonne ; filtres. Décisions ouvertes : projet/priorité par tâche | CF |
| [x] | **Kanban — glisser-déposer non fonctionnel (spec/06, feedback tests)** : le vrai bug était `dragDropEnabled` (Tauri, défaut `true`) qui intercepte nativement le drag et bloque le HTML5 DnD du webview — mis à `false` dans `tauri.conf.json` (corrige aussi le drag-drop de la vue Projets) ; ouverture de carte → fiche tâche (`TaskSheet.tsx`) | CF |
| [x] | **Kanban — colonnes + statut Fait (spec/06, feedback tests)** : retirer la colonne **Prioritaire** ; ordre **À faire → En cours → Fait → Archivé** ; ajouter le statut **`Fait`** = section `## Fait` où **cocher `[x]` déplace la tâche** (décocher → `À faire`, déposer dans `Fait` coche — bijection portée par `move_task`, réutilisée par `set_checked`) ; priorité uniquement via `!` + **tri intra-colonne** (haute en haut) + **filtre priorité** ; **migration** `## Prioritaire` → `## À faire` et `[x]` épars → `## Fait`, auto-appliquée + persistée à la première lecture (`get_todos`/`get_all_todos`) ; MAJ bloc tâches accueil (spec/10, plus de Prioritaire) | UC |
| [x] | **Notes — archivage auto transcription + « Afficher les archives » (spec/07/05/10, feedback tests)** : après `/resolve` + ingestion réussie (compte-rendu), passer la **note brute** `status: archived` (compte-rendu reste `active`, `vault::archive_raw_note_by_recording_id`, best-effort) ; **masquer les archivées** de l'arbre Notes (`VaultNode.status`) ET des Récents (`list_recent_notes`, filtré avant troncature) ; bouton toggle **« Afficher les archives »** (vue Dossiers, estompé + badge) ; désarchivage via Properties (`status`, déjà fonctionnel) | UC |
| [x] | **Notes — archives : 3 correctifs (spec/07/13, feedback tests)** : (a) **archive aussi la transcription de CONTEXTE** après `build_context_from_transcription` réussi (`archive_raw_note_by_recording_id`, même mécanisme que l'ingestion) ; (b) **toggle « Afficher les archives » sorti du sélecteur Folders/Projects** (2 vues seulement) → déplacé en **pied d'arbre**, identique dans les 2 vues ; (c) **`ProjectNote` gagne `status`** — la vue Projects filtre désormais les archivées comme Dossiers (bug corrigé), même estompé + badge visuel | UC |
| [x] | **Tâches — retrait de la vue Markdown de l'onglet (spec/06, feedback tests)** : redondante avec l'écran Notes (même `Todo.md`, même éditeur) — le bouton « Markdown » devient un simple raccourci (`selectFile` + navigation) qui ouvre le fichier dans Notes ; le Kanban reste la seule vue de la page Tâches | UC |
| [x] | **Todo.md structuré statut → projet → priorité (spec/06, feedback tests)** : chaque section regroupe ses tâches par `+Projet` sous `### Projet` (alphabétique, « Sans projet » en dernier, pas d'en-tête si un seul groupe), triées par `!priorité` dans chaque groupe ; en-têtes entièrement dérivés (jamais la source de vérité) donc auto-cohérents à chaque écriture (`group_lines_by_project`, `merge_tasks`) — même mécanisme de migration/auto-guérison que Prioritaire→À faire | UC |
| [x] | **Bug — retours à la ligne fantômes en tête de note (spec/07)** : ouvrir une note pouvait déclencher une sauvegarde sans aucune frappe (resynchro `body`/`noteKey` de `NoteEditor` sur 2 rendus → `onChange` déclenché par un correctif de contenu identique), qui grossissait de 1 saut de ligne à chaque round-trip `serialize`/`parse` (ce dernier ne retirait qu'**un seul** `\n` en tête au lieu de tous) — corrigé des deux côtés : `parse()` retire tous les `\n`/`\r` de tête (idempotent, auto-guérit les notes déjà touchées) + `handleBodyChange` (Notes.tsx) ignore les sauvegardes dont le contenu est identique à celui déjà chargé (évite aussi de fausser le tri des Récents par mtime) | UC |
| [x] | **Kanban — filtre projet invisible (spec/06, feedback tests)** : le filtre projet existait mais sa liste ne venait que des marqueurs `+Projet` des tâches → invisible tant que rien n'était tagué. Liste = **union `list_projects` (vault, spec/07) + projets des tâches**, rafraîchie sur `notes-updated` ; alimente aussi l'autocomplétion projet de la fiche tâche | T |
| [x] | **Kanban — filtre par réunion à la place du filtre projet (spec/06, feedback tests)** : le filtre projet marche mal et n'est pas le bon axe — remplacé par une **combobox avec recherche** sur les réunions d'origine des tâches (provenance `[[…]]` de `Todo.md`, champ `source_note`), triées par date desc, entrée « Sans réunion » ; `list_projects` ne sert plus qu'à l'autocomplétion de la fiche tâche | T |
| [x] | **Nouvelle note → `alfred-raw` + dossier paramétrable + Transcription en bas (spec/07/11, feedback tests)** : le bouton « Nouvelle note » créait dans `raw/` codé en dur (ancien défaut) → config **`new_note_folder`** (défaut `alfred-raw`, ligne Réglages → Notes, composant `FolderConfigRow` généralisé depuis le dossier d'enregistrement) ; section **Transcription** (gestionnaire de modèles, devenue la plus haute) déplacée **tout en bas** des Réglages | T |
| [x] | **Tâches extraites → `+Projet` auto (spec/06/05, feedback tests)** : l'ingestion identifiait déjà les projets de la réunion (frontmatter du compte-rendu) mais les perdait en écrivant les tâches dans `Todo.md` — `IngestTask.project` posé en marqueur `+Projet` (premier projet identifié), les tâches de réunion deviennent filtrables par projet sans tagage manuel | T |
| [x] | **Menu profil haut-droite → retirer + profil local (spec/10/11, feedback tests)** : menu ambigu retiré (déjà fait) ; **profil local** ajouté (`ProfileSection`, Réglages) — prénom + avatar (image → data URI) en config, sans compte serveur ; réutilisé via bouton « M'assigner » + badge « moi » (fiche tâche, Kanban, vue Markdown, spec/06) et chip « Moi » parmi les participants (Properties, spec/07). Signature de partage (spec/18) non faite — cross-service, signalé pour plus tard | CF |
| [x] | **Tâches 2e passe — vues + fiche + provenance/contexte (spec/06/05/07b, feedback tests)** : (a) bascule **Kanban / Markdown** (sections repliables, Archivé replié par défaut) sur la même liste `Todo.md` ; (b) **fiche tâche** (`TaskSheet.tsx`) ouvrable depuis Kanban et Markdown — sous-puces libres, champs inline `+Projet`/`!priorité`/`⏱estimation` (filtrables), description longue, tout en blocs indentés compatibles Obsidian (`todo_md.rs` réécrit : `TaskFields`/`TaskBlock`) ; (c) **provenance** = wikilink `[[compte-rendu ou note brute]]`+date posé par l'ingestion, lien graphe automatique (résolution wikilink standard) + `/graph?focus=` pour centrer ; (d) bouton **« Rassembler le contexte »** (`gather_task_context`) = action IA à la demande réutilisant la boucle RAG du chat (spec/07b) | CF |
| [x] | **Page partagée — footer marque (spec/18)** : « Partagé via Alfred » = lien vers `alfred.do-now.io` + logo (servi par le backend `GET /logo.png`, favicon inclus) | T |
| [x] | **Icônes de type (spec/07)** : glyphes Material Design (`react-icons/md`, `utils/noteType`) — SVG inline, identiques Windows/macOS/Linux ; distinction **audio** (transcription datée d'un enregistrement) / **note brute** / **synthèse Alfred** + tâches/contexte/note ; arbre + Récents + vue Projets. *(La variante « feuille + glyphe interne » (SVG maison) essayée puis abandonnée — retour utilisateur : trop petite/discrète — revenu aux glyphes Material, plus visibles.)* | CF |
| [x] | **Clic droit sur un dossier — créer/renommer/supprimer (spec/07)** : le menu contextuel est câblé sur les dossiers mais ne s'affiche jamais (vestige non fonctionnel, seul le menu fichier se rend) ; aucune commande Tauri dossier (`rename_note_file` suffixe `.md`, `delete_note_file` échoue sur un répertoire). À faire : `create_folder`/`rename_folder`/`delete_folder` + menu contextuel dossier réel + entrée « Nouveau dossier ». Inclut aussi le glisser-déposer **interne** d'une note vers un dossier pour la déplacer | CF |
| [x] | **Clic droit incohérent — Récents (spec/07, feedback tests)** : la liste « Récents » de la sidebar (`App.tsx`) n'avait aucun `onContextMenu` → menu natif du navigateur (Retour/Actualiser/Imprimer…) au lieu du Renommer/Supprimer de l'arbre. Menu extrait en composant partagé `NoteContextMenu` (arbre + Récents) ; corrige aussi l'icône empilée au-dessus du texte au lieu d'être sur la même ligne (Tailwind `svg { display: block }` — fix `display: flex` + `alignItems: center`) | CF |
| [x] | **Vue Projets — paire transcription/compte-rendu repliée par défaut (spec/07, feedback tests)** : la transcription appariée s'affichait toujours en retrait sous le compte-rendu (2 lignes par entrée) ; un chevron (▶/▼) la déplie/replie à la demande — une seule ligne par défaut, comme la vue Dossiers | CF |
| [ ] | **Glisser-déposer de fichiers externes dans Notes (spec/07)** — **Hors v1, décision produit** : glisser un fichier depuis le Finder/l'Explorateur (PDF, image…) sur l'arbre ou le contenu ne fait rien aujourd'hui (`dragDropEnabled: false` désactive le DnD natif de fichiers OS ; aucun handler `dataTransfer.files`). Volontairement pas fait pour la v1 (feedback tests, CF) | |
| [ ] | **Nom du dossier des transcriptions brutes — décision ouverte (spec/07)** : `alfred-raw` est le nom par défaut actuel, jamais validé comme définitif produit ; à rouvrir si besoin, en tenant compte de la migration des vaults déjà créés chez les utilisateurs en test | |
| [x] | **Voix du majordome — passe éditoriale (spec/10)** : le ton « majordome » n'existe que sur les labels d'état (« À votre service », « Je cogite… »…) ; relire l'ensemble des textes (boutons, placeholders, erreurs, onboarding) pour un ton cohérent de bout en bout, sans nuire à la lisibilité des messages d'erreur | CF |
| [x] | **Recherche dans le fichier courant (spec/06/07/10)** : Notes — Ctrl/Cmd+F dans l'éditeur (`@codemirror/search`, panneau FR, bouton loupe, raccourci global écran Notes) ; Tâches — champ « Rechercher… » qui filtre les cartes Kanban en direct (titre+responsable, insensible casse/accents). Recherche **locale** uniquement (globale toujours hors v1) | T |
| [x] | **Settings** refonte (accès IA ✓, Whisper ✓, Vapi/Google/Places/calendrier/ingest CLI retirés ✓, défauts `alfred-*` ✓) | UC |
| [x] | **Onglet Feedback** (formulaire + `submit_feedback`) | UC |
| [x] | **Widget feedback discret** (topbar) : popover textarea + envoi rapide (catégorie `quick`) ; champ `view` (vue courante) bout en bout (widget → `submit_feedback` → backend → Postgres) ; onglet retiré de la sidebar (écran gardé, lien « formulaire détaillé »). Déployé et vérifié en prod | T |
| [x] | **Visite guidée — retouches de texte + bug halo décalé (spec/13, feedback tests)** : point d'état/clôture/notes reformulés (moins de répétitions, retrait de « regroupés par projet » et « je connais votre univers ») ; le rappel « comment enregistrer » ne vit plus que dans `record-cta` (retiré de l'étape chat de la visite, redondant) ; **bug corrigé** — le halo de `record-cta` apparaissait décalé par rapport à `hero-card` quand la bannière « Supprimer les données de démo » s'affichait au-dessus (async) : `Spotlight` mesurait sa cible via `ResizeObserver` seul, qui ne détecte qu'un changement de **taille**, jamais un décalage de **position** causé par un voisin — remplacé par un suivi en boucle (`requestAnimationFrame`, avec mesure synchrone immédiate au montage pour éviter tout flash) | UC |
| [x] | **`/resolve` — survol d'une carte à vérifier surligne sa citation dans le texte (feedback tests)** : `NoteEditor` gagne un prop `highlightQuote` (décoration CodeMirror, `StateEffect`/`StateField`, scroll into view) ; `Resolve.tsx` le pilote au survol des cartes `transcription_fixes`/`unclear_sentences` | UC |
| [x] | **Partage — texte du consentement resserré (feedback tests)** : le `window.confirm` (déjà à usage unique, jamais réaffiché ensuite) reformulé pour mener avec le risque (« accessible à toute personne ayant le lien, êtes-vous sûr ? ») plutôt qu'avec une question neutre ; décision explicite de garder le `window.confirm` natif (cohérent avec le reste de l'app) plutôt qu'une nouvelle brique modal pour un seul bouton | UC |
| [x] | **Bandeau démo — wording corrigé (feedback tests)** : « Ces données sont des exemples »/« This data is example content » impliquait que TOUT ce qui est affiché est un exemple, alors que les vraies données de l'utilisateur coexistent avec le contenu de démarrage semé. Reformulé en « Il y a des données d'exemple »/« There's some example content in here » | UC |
| [x] | **Tâches — mise en évidence des tâches jamais ouvertes (spec/06, feedback tests)** : badge sur une carte jusqu'à ce qu'elle soit ouverte au moins une fois, calculé **une seule fois par ouverture de la page** (`src/utils/tasksSeen.ts`, `localStorage`) — pas recalculé à chaque rafraîchissement de la liste pendant qu'on est déjà sur la page. Premier lancement : le passif existant est semé comme « déjà ouvert » pour ne pas tout illuminer d'un coup | UC |
| [x] | **Bug — « Rassembler le contexte » répond en français malgré une UI anglaise (spec/07b, feedback tests)** : `gather_task_context` (`lib.rs`) rédigeait la question envoyée à `answer_question` **en dur en français** — la consigne de langue du chat infère depuis le **contenu envoyé** (même mécanisme que spec/17/22), donc Claude répondait en français quelle que soit `app_language`. La question suit désormais `app_language`, comme le reste du texte qu'elle enveloppe (titre de tâche, provenance) | UC |

## Phase D — Nettoyage (retrait hors-v1)

| | Tâche | Qui |
|---|---|---|
| [x] | Désactiver / retirer les modules `auth`, `calendar`, `suggestions`, `phone_calls` (`ingest` CLI déjà supprimé — spec/05) + tables SQLite associées (migration 008) + UI morte (WeekPanel, BookingDemo, BriefingTask, SuggestionCard, PhoneCallModal, stores) + deps orphelines (base64/sha2/hex) + `.env.example` Google | UC |
| [x] | Retirer les routes `/meetings`, `/calendar` (faites avec la nav Phase C ; `/ai-actions` conservée — écart documenté spec/10) | UC |
| [x] | Mettre à jour les défauts de dossiers (`alfred-raw` ✅, `alfred-intelligence/Todo.md` ✅) | UC |
| [x] | **Nettoyage vestige `raw/` dans le vault (spec/07/11, feedback tests)** : les vaults réutilisés gardent un `raw/` (`raw/audios/`) orphelin de l'ancien défaut `raw/audios` (le code écrit dans `alfred-raw/`). `migrate_legacy_raw_folder`/`migrate_legacy_todo_file` (`vault.rs`) — déplace le contenu de `raw/` (aplati, y compris `raw/audios/`) → `alfred-raw/` (suffixe `_2`/`_3`… si collision, jamais d'écrasement), supprime les dossiers vidés ; idem `wiki/Todo.md` → l'emplacement configuré, seulement si absent. Branché dans `scaffold_vault` (idempotent) **et rejoué à chaque lancement** pour un vault déjà connu — sinon un utilisateur déjà configuré avant cette migration ne la déclencherait jamais (`set_vault_path` n'est appelé qu'au choix explicite du dossier) | UC |
| [x] | **`Todo.md` — en-têtes toujours en FR même en UI anglaise (spec/06/21, feedback tests)** : l'écriture des sections restait figée en FR (seule la reconnaissance en lecture était bilingue, décision différée documentée dans le code). `todo_md::section_label(canonical, lang)` localise désormais l'en-tête à l'écriture (`merge_tasks`/`move_task`/`archive_task`/`migrate`/squelette de démarrage), à partir de clés de section stables en interne — `move_task` comparait ses en-têtes par égalité de chaîne littérale FR, corrigé pour comparer via `canonical_section` (robuste à la langue d'écriture) | UC |
| [x] | **Corrections de transcription perdues après finalisation (spec/17 §3, feedback tests)** : `/resolve` → Finaliser mettait à jour le compte-rendu à partir du texte corrigé, mais ni la note de transcription brute ni `transcriptions.raw_text` n'étaient réécrits — rouvrir la note brute (ou relancer une analyse) montrait encore la sortie Whisper d'origine. `run_ingestion_core` reporte désormais le texte corrigé sur les deux dès l'entrée en finalisation (`update_raw_note_body_by_recording_id`, retrouvée par `recording_id`) | UC |

## Phase E — Packaging & distribution

| | Tâche | Qui |
|---|---|---|
| [x] | **CI GitHub Actions — build desktop 3 OS** (`.github/workflows/desktop-build.yml`) : Windows (msi/nsis) + macOS (dmg arm64 — pas d'Intel, runners macos-13 plus provisionnés) + Linux (deb/rpm/AppImage), **non signé** ; manuel + tags `v*` ; **release GitHub auto sur tag** avec les binaires attachés ; base sqlx recréée en CI ; **aucun modèle embarqué** (téléchargé à l'onboarding) | T |
| [ ] | **macOS** : entitlements v1 (retirer apple-events, + `NSScreenCaptureUsageDescription`) ; signature Developer ID + notarisation | |
| [x] | **Windows** : build Whisper + WebView2 (fait) ; signature **Authenticode** via **SSL.com eSigner** (OV DONOW validé, cloud HSM) — **vérifiée en réel** : installeur `Alfred_x.y.z_x64-setup.exe` téléchargé depuis une release taguée, SmartScreen affiche bien l'éditeur **« DONOW »** (plus « Unknown publisher »). Signature en étape CI à part (`scripts/sign-windows.ps1`, pas un hook `signCommand` Tauri — abandonné après plusieurs échecs opaques) dans un dossier de staging (CodeSignTool refuse `output_dir_path == dossier source`) ; détection d'échec par hash SHA256 (CodeSignTool renvoie exit 0 même en échec). SmartScreen affichera encore un avertissement (OV, pas EV) jusqu'à accumulation de réputation — compromis de coût assumé, spec/12 | UC |
| [x] | Aligner le label launch-at-login `io.alfred.app` → `com.alfred.app` | CF |
| [x] | **Icône d'app à mettre à jour (feedback tests)** : le jeu `src-tauri/icons/*` (32/128/@2x/.ico/.icns + Windows Appx/iOS/Android) régénéré via `npx tauri icon` depuis `alfred-logo-minimal.png` (portrait seul, sans mot-symbole, fond transparent) — pas encore vérifié en rendu réel bureau Windows/macOS (à faire manuellement au prochain build) | CF |

## Phase F — Produit & ouverture (spec à écrire)

| | Tâche | Qui |
|---|---|---|
| [ ] | **Site web (`alfred.do-now.io`) — spec/19** : rien n'existe (ni code, ni spec) — le footer des pages partagées (spec/18) pointe déjà vers ce domaine. Écrire la spec (contenu, hébergement) avant de coder | |
| [ ] | **Rendre le projet open source — spec/20** : décisions produit/légales à prendre d'abord (licence, dépôt cible, ce qui doit rester privé) avant d'écrire la spec | |

---

*Mis à jour au fil de l'eau. Nouveau besoin non couvert par une spec → ajouter la
spec d'abord, puis la tâche ici.*
